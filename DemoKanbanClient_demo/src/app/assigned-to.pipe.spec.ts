import { AssignedToPipe } from './assigned-to.pipe';

describe('AssignedToPipe', () => {
  it('create an instance', () => {
    const pipe = new AssignedToPipe();
    expect(pipe).toBeTruthy();
  });
});
